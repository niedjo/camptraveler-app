/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    images: {
      unoptimized: true, // Optionnel, si vous utilisez des images
    },
  };
  
  export default nextConfig;
  
